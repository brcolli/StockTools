import sys
import importlib
from PyQt5.QtWidgets import QApplication, QWidget, QPushButton, QVBoxLayout, QLineEdit, QLabel, QMainWindow
from tkinter import Tk
from tkinter.filedialog import askopenfilename
import pandas as pd
import KellyCriterion

KCUI = 0

# window = tk.Tk()
#
# frame_p = tk.Frame()
# label_p = tk.Label(master=frame_p, text="Enter the Probability of winning (0.00-1.00)")
#
#
# probability = tk.Entry(text="Probability of winning (0.00-1.00)")
# odds = tk.Entry(text="Probability of winning (0.00-1.00)")
# bankroll = tk.Entry(text="Probability of winning (0.00-1.00)")
# fractional_kelly = tk.Entry(text="Probability of winning (0.00-1.00)")
#
# greeting.pack()
# probability.pack()
# odds.pack()
# bankroll.pack()
# fractional_kelly.pack()
#
#
# window.mainloop()

class QTManager(QMainWindow):
    pass
def main():
    pass